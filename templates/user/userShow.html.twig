{% extends 'commons/base.html.twig' %}
    {% block body %}
        <meta name="keywords" content="golf, professeur, cours, coach, gilles, arnaud, bordeaux, médoc, gironde, sport"/>
        {% include 'commons/_headerLittle.html.twig' %}
        <main class="headUser">
            <section class="userShowDisplay filtreHeader">
                <div class="userShowContainerhowContainer">
                    <h1>{% if user.genre("femme") %} Mme
                        {% else %} M.
                        {% endif %}
                        {{ user.lastName }}  {{ user.firstName }}</h1>
                    <p class="userShowP">
                    <div>Tél. : {{ user.phone }}   -    Email : {{ user.email }}</div>
                    </p>
                    <p class="userShowP">Index :  {{ user.level }} -   Club :  {{ user.club }}</p>
                    {% if user.creditDuration > 0 %}
                        <div> Solde de tickets non utilisés à ce jour {{ user.creditDuration }}</div>
                    {% endif %}
                </div>
            </section>
            <section>

                {#     <a class="userButtonUpdate" href="{{ path('user_show',{'id':user.id}) }}">modifier</a>

                {#   <a class="button buttonConnect" href="{{ path('home') }}">Se connecter</a>
                      <input type="submit" name="Se connecter"  href="{{ path('home') }}" class="buttonConnect">#}
                <a class="button buttonLeft" href="{{ path('course') }}">Les cours</a>
            </section>
        </main>
    {% endblock %}